import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    controlsRow: {
        flexDirection: 'row',
        alignSelf: 'stretch',
        justifyContent: 'space-between',
        margin: 10,
    },
});