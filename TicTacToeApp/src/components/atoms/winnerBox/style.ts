import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },
    winnerText: {
        fontSize: 34,
        fontWeight: 'bold',
        color: '#f54a28',
        margin: 5,
    },
});